// api.ts — All fetch calls to the FastAPI backend
// TODO: analyzeImage(formData) -> AnalyzeResponse
// TODO: simulateImpact(formData, params) -> SimulateResponse
// TODO: diffImages(before, after) -> DiffResponse
// TODO: getHistory(regionId?) -> Analysis[]
// TODO: getReport(id) -> Report

const BASE_URL = import.meta.env.VITE_API_URL ?? "https://inlionde4n-disaster-platform-api.hf.space"
